// standard headers:
#include <iostream>
#include <string>

// user-defined headers:
#include "check_palindrome.h"
#include "util.h"

bool is_palindrome(const std::string &str) {
  bool res{false};
  // .. solution

  return res;
}